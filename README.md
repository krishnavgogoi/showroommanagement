M Motors is a full-stack car dealership application built in C++. The core logic — car inventory, tax calculation, test drive booking, and customer management — is compiled to WebAssembly via Emscripten and powers an interactive browser-based frontend. A companion native C++ admin binary handles backend operations like viewing sales logs and managing bookings via a CSV bridge workflow.
